#include<stdio.h>
int main()
{
    int nr,nc,r,c;
    printf("enter the no of rows and colums");
    scanf("%d %d",&nc,&nc);
    for(r=1;r<=nr;r++)
    {
        for(c=1;c<=nc;c++)
        printf("* ");
        {
            printf("\n");
        }
    }
}